---
date: 2026-04-24
session: 03
title: "Professor feedback changes everything. Normal mapping clicks. Scope locked."
tags: [feedback, professor, scope, normal-mapping, pipeline, decisions]
---
# Entry 03: Professor feedback changes everything

## Normal mapping finally clicked

Before the help session, spent time with the GameMaker article and Perplexity. The line that unlocked it:

> "In 2D it will be the normal direction for that particular pixel being shaded."

In 3D, normals come from geometry. The mesh tells you which way each face points. In 2D there is no geometry. Every pixel sits on the same flat plane, so every pixel would have the same normal: (0, 0, 1), pointing straight at the camera. No depth, no response to light angle, flat cardboard.

The normal map fixes this by storing fake normals per pixel as an RGB image:
- Red = facing right (+X)
- Green = facing up (+Y)
- Blue = facing toward camera (+Z)
- (128, 128, 255) = flat, facing straight at camera

The shader reads this instead of assuming (0, 0, 1) everywhere. Now each pixel has a unique direction and responds to light differently. A cheek curves away from the light. A shoulder catches it. All without any 3D geometry.

**Three ways to make a normal map for a 2D sprite:**
1. Hand-paint RGB colours directly (slowest, most control)
2. Paint a greyscale height map → software generates normals from gradients (fastest, intuitive)
3. 3D model → render normal pass (studio method, overkill)

Method 2 is what we were going to do. 

---

## The professor's feedback (Christopher Peters)

Got written feedback on the spec. Four answers, one warning.

**Q1: p5.js vs Processing:**
Confirmed fine. "The algorithmic level (implementation and evaluation related) are what matter in the course." Platform doesn't matter. The math matters.

**Q3: Normal map approach:**
"Procedural approaches much more interesting since this is a computer graphics course."
Don't hand-paint normal maps. Don't even use a software generator. **Derive the normals in the GLSL shader itself**, from a greyscale height map, using gradient math. That's the academically interesting version.

**Q4: Three rooms:**
"Use your time to create depth rather than breadth. Choose the minimal scenario necessary to demonstrate your technique and use the saved time to investigate aspects such as evaluation approaches."
Fewer rooms. One room done properly is better than three rooms done shallowly.

**The warning:**
"Additional aspects such as story and character design are not relevant to the course. Only the 'Normal-Mapped Sprite Pipeline in the browser' technique, so be careful about that."
"It is easy for this sort of project to hit a B or C grade."

This is the clearest signal I've received. The story, the branching narrative, the character names and horror atmosphere: none of it is graded. Spending time on that is spending time away from what gets the A.

---

## What this changes

**Story and narrative:** minimal. One room. The scenario exists only to demonstrate the technique: moving light, environment responding, Charlie Chat shadow growing. That's it.

**Normal maps:** procedural derivation in the shader. A greyscale height map of the environment goes in. The shader computes the normal from the gradient of that height map. No software tools for normal generation. Pure math.

**Characters:** flat 2D cartoony, no normal mapping. This was already decided today (see below) and the professor confirms the direction: focus on the environment pipeline, not the characters.

**Evaluation:** more important than I thought. This is part of the A-grade criteria. The user study comparing full pipeline vs flat rendering needs to be properly designed with defensible metrics.

**Literature:** not just citations. Actually engage with the papers. Know what Hart (1996) says about sphere tracing. Know what the Reinhard tone mapping paper actually argues.

---

## The design decision locked today (before professor feedback)

**Environment gets normal mapping. Characters stay flat 2D.**

 The reasoning:

1. **Time.** A 6-frame walk cycle in 4 directions = 24 normal maps to paint by hand. Keeping characters flat cuts art production time by ~80%.

2. **Aesthetics.** Flat cartoony characters in a physically-lit environment creates visual dissonance: the character looks like it doesn't belong in the world. That dissonance is the horror. It reinforces the "uncanny" tone without any extra rendering work.

The professor's feedback confirms this is the right call: environment pipeline is the contribution, not the characters.

---

## Rendering pipeline locked (4 passes)

```
Pass 1: Environment
  Input: background texture + greyscale height map
  Process: GLSL fragment shader (derive normal from height gradient, compute N·L diffuse)
  Output: lit environment to framebuffer

Pass 2: Characters
  Input: flat sprites (Charlie, NPCs)
  Process: draw directly, no shader (maybe distance-based flat tint for basic light response)
  Output: composited on top of Pass 1

Pass 3: SDF Shadow (Charlie Chat)
  Input: Charlie Chat's screen position as uniform
  Process: SDF raymarcher (procedural circle SDF, Quilez soft shadow formula)
  Output: shadow mask composited over scene

Pass 4: Post-Processing
  Input: full composited scene
  Process: bloom, vignette, film grain, Reinhard tone mapping, chromatic aberration
  Output: final frame
```

This is now the canonical pipeline. Everything gets built to this spec.

---

## What "procedural normal derivation" means

Instead of providing a pre-computed normal map texture, provide a **greyscale height map** a simple image where white = surface raised toward the camera, black = surface pushed away.

In the GLSL shader, I compute the gradient of this height map using neighbouring pixels (Sobel filter or screen-space derivatives). The gradient tells about the slope. The slope gives the normal direction.

This is more academically interesting because:
- No separate normal map texture to author or store
- The derivation happens live in the shader every frame
- It proves that I understand the relationship between height fields and surface normals  the actual mathematics

I don't fully understand Sobel yet. That's the next thing to learn.

---

## What I still don't understand

- Sobel filter  what it is, how it computes gradients from a texture
- How to structure a multi-pass pipeline in p5.js (createFramebuffer, how textures pass between passes)
- The full raymarching loop I understand each step individually but haven't written it

These are the next three things the code will teach me.

---

## Literature cited this session

| Concept | Source |
|---|---|
| Normal maps store surface direction per pixel as RGB (R=+X, G=+Y, B=+Z) | [1] Akenine-Möller et al., *Real-Time Rendering*, 4th ed., Ch. 9 |
| Deriving normals procedurally in the shader from a height map gradient | [5] Schüler, C. "Normal Mapping Without Precomputed Tangents," 2013. thetenthplanet.de |
| N·L diffuse in the rendering pipeline | [1] Akenine-Möller et al., *Real-Time Rendering*, 4th ed., Ch. 9.1 |
| Reinhard tone mapping in post-processing | [6] Reinhard et al., "Photographic tone reproduction," *ACM Trans. Graph.*, 2002 |
| SDF shadow raymarcher in the pipeline | [3] Quilez, "Soft shadows in raymarched SDFs," 2010 + [4] Hart, "Sphere tracing," 1996 |

## AI usage log

| Date       | Tool                                    | Used for                                                                                          |
| ---------- | --------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 2026-04-24 | Perplexity (Claude Sonnet 4.6 Thinking) | Normal mapping understanding, design decision (flat chars vs env normal maps), pipeline structure |


---

## Next session goals

1. Understand what a Sobel filter is and why it gives you normals
2. First code: p5.js in WebGL mode, single shader, mouse position → color gradient. Prove the pipeline works.
